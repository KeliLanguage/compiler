List.of(A Type) 
    =   tag.new
    .or(tag.cons(record.current(A) next(List.of(A))))

(this Int).+(that Int) = ffi.javascript("$this + $that").as(Int)

{A Type}
(this List.of(A)).length | Int = 
    this.
        new?
            (0)
        cons?
            (1.+(this.carry.next.length))

{A Type}
(xs List.of(A)).add(x A) | List.of(A) = 
    xs.
        new? 
            (List.cons.carry(record.current(x) next(List.new)))
        cons? 
            (List.cons.carry(record.current(xs.carry.current) next(xs.carry.next.add(x))))
    
=List.new.add(1).add(2).add(3).add(4).length

====

4