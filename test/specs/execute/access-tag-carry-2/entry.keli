Human = choice
    .Boy    
    .Baby($.cuteness(Int))
    .Worker($.salary(Int))

julia=Human.Baby($.cuteness(99))

=julia.
    case(.Boy)       : (0) 
    case(.Baby(b))   : (b.cuteness) 
    case(.Worker(w)) : (w.salary)
