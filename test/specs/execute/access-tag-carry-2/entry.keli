Human = choice
    .Boy    
    .Baby($.cuteness(Int))
    .Worker($.salary(Int))

julia=Human.Baby($.cuteness(99))

=julia.
    if(.Boy)        then (0) 
    if(.Baby(b))    then (b.cuteness) 
    if(.Worker(w))  then (w.salary)
