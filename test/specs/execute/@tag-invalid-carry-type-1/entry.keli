List.of(A Type) = tags.
    case(.New)
    case(.Cons($.current(A) next(List.of(A))))


z = List.Cons($.
        current (1) 
        next    (List.Cons($.
            current ("123") 
            next    (List.New))))