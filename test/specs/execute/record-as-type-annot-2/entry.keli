Boolean = tags.
    case(true)
    case(false)

List.of(A Type) = tags.
    case(cons.current(A) next(List.of(A)))
    case(nil)

{A Type}
(this Int).hey | object.accepted(List.of(A)) notAccepted(List.of(A))
    = object.accepted(List.nil) notAccepted(List.nil)

= 5.hey