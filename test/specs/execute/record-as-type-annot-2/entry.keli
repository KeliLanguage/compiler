Boolean = tags.
    #(true)
    #(false)

List.of(A Type) = tags.
    #(cons.current(A) next(List.of(A)))
    #(nil)

{A Type}
(this Int).hey | record.accepted(List.of(A)) notAccepted(List.of(A))
    = record.accepted(List.nil) notAccepted(List.nil)

= 5.hey