Boolean = choice
    .True    
    .False

List.of(A Type) = choice
    .Nil
    .Cons($.current(A) next(List.of(A)))

{A Type}
(this Int).hey |  $.accepted(List.of(A)) notAccepted(List.of(A))
    =  $.accepted(List.Nil) notAccepted(List.Nil)

= 5.hey