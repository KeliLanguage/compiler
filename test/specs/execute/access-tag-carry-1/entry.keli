(this Int).+(that Int) | Int = ffi.javascript("k$this + k$that").as(Int)

Color = choice
    .Red    
    .Green(Int)

x = Color.Green(2)
y = Color.Green(5)

=x.
    if(.Red) then
        (10) 
        
    if(.Green(v1)) then
        (y.
            if(.Red) then
                (99)
            if(.Green(v2)) then
                (v1.+(v2)))

