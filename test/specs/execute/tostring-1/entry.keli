List.of(A Type) = tags.
    case(.Nil)
    case(.Cons($.current(A) next(List.of(A))))

{A Type}
(this List.of(A)).toString =
    this.
        case(.Nil):
            ("It is a nil")
        case(.Cons):
            ("It is not empty dude")

= List.Nil
= List.Cons($.current(1) next(List.Nil))