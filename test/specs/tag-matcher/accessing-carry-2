human = 
        (tag.# ( boy ))
    .or (tag.# ( baby ) carry (record.cuteness ( int )))
    .or (tag.# ( worker ) carry (record.salary ( int )))

x=human.baby.carry(record.cuteness ( 2 ))

=x.boy? ( 0 ) baby? (x.carry.cuteness) worker? (x.carry.salary)

====

2