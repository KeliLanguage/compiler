List.of(A Type) = tags.
    #(new)
    #(cons.current(A) next(List.of(A)))


z = List.cons.
        current (1) 
        next    (List.cons.
            current ("123") 
            next    (List.new))

==== 

KErrorTypeMismatch