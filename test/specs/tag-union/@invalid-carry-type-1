List.of(A Type) 
    =   tag.#(new)
    .or(tag.#(cons) carry(record.current(A) next(List.of(A))))


z = List.cons.carry(record.
    current (1) 
    next    (List.cons.carry(record.
        current ("this one should be int") 
        next    (List.new))))

==== 

KErrorTypeMismatch