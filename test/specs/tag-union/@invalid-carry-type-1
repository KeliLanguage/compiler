List.of(A Type) 
    =   tag.new
    .or(tag.cons(record.current(A) next(List.of(A))))


z = List.cons(record.
    current (1) 
    next    (List.cons(record.
        current ("123") 
        next    (List.new))))

==== 

KErrorTypeMismatch