List.of(A Type) 
    =  (#new)
    .or(#cons.current(A) next(List.of(A)))

(this Int).+(that Int) = ffi.javascript("$this + $that").as(Int)

{A Type}
(this List.of(A)).length | Int = 
    this.
        if(#new):
            (0)
        if(#cons.next(n)):
            (1.+(n.length))

z = List.of(Int).#cons.current(1) next(List.of(Int).#cons.current(2) next(List.of(Int).#new))

= z.length
====

2