List.of(A Type) 
    =   tag.new
    .or(tag.cons(record.current(A) next(List.of(A))))

{A Type}
(this List.of(A)).add(element A) | List.of(A) = 
    this.
        new? 
            (List.cons(record.current(element) next(List.new)))

        cons(x)? 
            (List.cons(record.current(x.current) next(x.next.add(element))))
    
=List.new.add(1).add("4")

====

KErrorTypeMismatch